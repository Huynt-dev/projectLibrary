a(href="/add") Thêm sách mới
form(action="/search" method="get" )
  input(type="search" name="q" value=queryValue)
  //`${q ? q : ''}`
  button(type="submit") Tìm kiếm

ul
  each value in library
    li= value.title
      a(href="book/" + value.id) Thông tin
      span      
      a(href="book/" + value.id + "/edit") Sửa
      span     
      a(href="book/" + value.id + "/delete") Xóa